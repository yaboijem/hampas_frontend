# Local direct API (or omit and use Vite proxy default /api):
# VITE_API_URL=http://localhost:8000/api
# Production (Vercel): omit or set /api so requests stay same-origin (see vercel.json rewrites).
# VITE_API_URL=/api
VITE_API_URL=/api
VITE_SENTRY_DSN=
